apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: portfolio
spec:
  # Every pod in the namespace starts with no ingress and no egress. Nothing is
  # reachable until an explicit allow rule says so.
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
