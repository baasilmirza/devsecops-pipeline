apiVersion: v1
kind: Secret
metadata:
  name: portfolio-app
  namespace: portfolio
type: Opaque
# Example shape only — values are placeholders. Never commit a real value.
# This file is the *input* to `scripts/seal-secret.sh`; the encrypted output is
# what would ever be applied/committed.
stringData:
  API_TOKEN: CHANGE_ME
  DATABASE_URL: postgres://CHANGE_ME
