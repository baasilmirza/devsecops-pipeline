apiVersion: v1
kind: ServiceAccount
metadata:
  name: portfolio-app
  namespace: portfolio
  labels:
    app.kubernetes.io/name: portfolio-app
    team: platform
# The application pod runs with this identity. It never needs the API server,
# so its token is not auto-mounted.
automountServiceAccountToken: false
